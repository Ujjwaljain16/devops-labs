apiVersion: v1
kind: Pod
metadata:
  name: cpu-throttle-demo
spec:
  containers:
    - name: stress
      image: polinux/stress
      command: ["stress"]
      args: ["--cpu", "2", "--timeout", "300s"]
      resources:
        requests:
          cpu: "100m"
        limits:
          cpu: "200m"

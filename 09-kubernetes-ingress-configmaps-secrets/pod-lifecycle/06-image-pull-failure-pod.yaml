apiVersion: v1
kind: Pod
metadata:
  name: image-pull-failure-demo
  labels:
    app: image-pull-failure-demo
    lifecycle-stage: image-pull-failure
spec:
  containers:
    - name: nginx
      # tag intentionally does not exist -> ErrImagePull / ImagePullBackOff
      image: nginx:this-tag-does-not-exist-v99

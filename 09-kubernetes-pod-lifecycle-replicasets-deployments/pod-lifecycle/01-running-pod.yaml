apiVersion: v1
kind: Pod
metadata:
  name: running-demo
  labels:
    app: running-demo
    lifecycle-stage: running
spec:
  containers:
    - name: nginx
      image: nginx:1.25-alpine
      ports:
        - containerPort: 80
